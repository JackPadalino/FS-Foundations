const paris = 'paris';
const france = 'france';

const vacationSpots = (spot1, spot2) => {
  if (spot1.length > spot2.length) {
    return spot1;
  }else{
    return spot2;
  };
};

function splitEm(num1,num2){
  return num1/num2;
};